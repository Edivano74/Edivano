import Stripe from 'stripe';import { env,json,db } from './_shared.mjs';
export default async(req)=>{if(req.method!=='POST')return json({error:'Method not allowed'},405);const stripe=new Stripe('sk_test_placeholder');const payload=await req.text();let event;try{event=stripe.webhooks.constructEvent(payload,req.headers.get('stripe-signature'),env('STRIPE_WEBHOOK_SECRET'))}catch(e){console.error('stripe signature',e.message);return new Response('Bad signature',{status:400})}if(event.type==='checkout.session.completed'){const s=event.data.object;const ref=String(s.client_reference_id||'');const cut=ref.indexOf('.');if(cut>0){const id=ref.slice(0,cut);const token=ref.slice(cut+1);const client=db(token);const {error}=await client.from('trip_requests').update({payment_status:'paid',paid_at:new Date().toISOString(),stripe_session_id:s.id}).eq('id',id);if(error)console.error('fulfillment',error.message)}}return json({received:true})}
